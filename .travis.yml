language: smalltalk
sudo: false

addons:
  apt:
    packages:
    - mongodb-org

# Select operating system(s)
os:
  - linux
  - osx

# Select compatible Smalltalk image(s)
smalltalk:
  - Pharo-6.0
  - Pharo-5.0
